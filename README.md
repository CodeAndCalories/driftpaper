# Driftpaper

Driftpaper — generative wallpapers you make in your browser and download, built with Three.js.

## Run locally

Just open `index.html` in a browser. No build step or server required.

## Deploy

Static site on Cloudflare Pages — no build needed:

- **Build command:** *(none)*
- **Build output directory:** `/` (repository root)
